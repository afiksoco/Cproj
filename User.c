/*
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>
#include "User.h"
#include "General.h"

void initUser(User* pUser, char* id)
{

    pUser->name = getStrExactName("Enter last name\n");
    pUser->lastName = getStrExactName("Enter last name\n");
    pUser->myReservations = NULL;
    pUser->id = id;
    pUser->phoneNumber = getStrExactName("Enter phone number");

}

char* getName()
{

}


void printUser(User* pUser)
{
    printf("Name : %s %s\n",pUser->name,pUser->lastName);
}*/
